# Browser configuration: use the project's public anon key, never a service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only: enables /api/book-metadata in Vite dev and the deployed API.
# Leave empty to run without external book metadata lookup.
YES24_API_KEY=
